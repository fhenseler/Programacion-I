int funcionFactorial(int a);

int funcionFactorial(int a){
    int i, acum = 1;
    for(i = a; i > 0; i--){
      acum *= i;
    }
     return acum;
}

int main()
{
int i, result, num;

  printf("Ingrese numero: ");
  scanf("%d",&num);

    result = funcionFactorial(num);
    printf("El factorial de %d es: %d", num, result);

  return 0;
}








result = funcionFactorial(num1);
printf("FACTORIAL (A!) = %d", result);
                getch();
                return 0;
                break;
